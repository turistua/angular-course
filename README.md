== Installation

1. npm install
2. bower install
3. install json-server (npm install -g json-server) and run it with command (json-server src/api/staff.json) from the working folder.
4. grunt compile, then grunt up